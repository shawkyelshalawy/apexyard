# Mubein ApexYard Ops Repo

This directory is the governance repo for Mubein.

Managed projects:
- `mubein-backend`
- `mubein-platform`
- `mubein-landing`

Use `workspace/` for live clones if you want the projects mirrored inside this ops repo.
